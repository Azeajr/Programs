#include <stdio.h>

int main(int argc, char *argv[]){
    double x = 12.3, y = 7.2, c = 3.2;
    double result;

    result = (x*x + y*y)/(a-b);

    printf("result: %f\n", result);
    return 0;
}
