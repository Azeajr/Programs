#include <stdio.h>
int main(int argc, char *argv[]){
    int row, col,
    int const boardsize=16, bandsize=8

    for(row=0;row<boardsize;row++){
        for(col=0; col<boardsize; col++){
            


        }
        printf("\n");
    }
}
